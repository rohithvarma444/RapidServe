pipeline {
    agent any

    environment {
        DOCKER_CREDENTIALS = credentials('rohithvarma- Dockerhub')
    }

    stages {
        stage('Docker Login'){
            steps{
                sh 'echo "login"'
            }
        }
    }
}