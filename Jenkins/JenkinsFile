pipeline {
    agent any

    environment {
        DOCKER_USERNAME = "rohithvarma"
    }

    stages {
        stage('testing') {
            steps {
                sh 'echo "hello world"'
            }
        }
    }
}